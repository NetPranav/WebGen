/**
 * Placeholder. `build.mts` writes the emitted components to `components/`
 * and rewrites this file before building, then restores this file
 * afterward.
 */
export default function Page() {
  return <main>export harness placeholder</main>;
}
